# HMA Compliance Stack Environment Variables
# Copy to .env.compliance and update with secure values

# ============================================================================
# CISO ASSISTANT CONFIGURATION
# ============================================================================

# PostgreSQL Database
CISO_DB_PASSWORD=change_me_to_secure_random_password_123

# Django Secret Key (generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())')
CISO_DJANGO_SECRET=change_me_to_50_character_random_string_abcdefg123456

# Admin Account
CISO_ADMIN_EMAIL=admin@huntmasteracademy.com
CISO_ADMIN_PASSWORD=change_me_to_secure_admin_password

# Email Notifications (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password
CISO_FROM_EMAIL=compliance@huntmasteracademy.com

# ============================================================================
# WAZUH SIEM CONFIGURATION
# ============================================================================

# Wazuh Indexer (OpenSearch)
WAZUH_INDEXER_PASSWORD=SecureIndexerPassword123!ChangeMe

# Wazuh API
WAZUH_API_USER=hma_wazuh_api
WAZUH_API_PASSWORD=SecureAPIPassword123!ChangeMe

# ============================================================================
# SHARED INFRASTRUCTURE (from main docker-compose.yml)
# ============================================================================

# These should match your existing .env file
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin
REDIS_PASSWORD=development_redis
DB_PASSWORD=development_password

# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# 1. Generate strong random passwords:
#    openssl rand -base64 32
#
# 2. Never commit this file with real passwords to Git
#
# 3. For production, use AWS Secrets Manager or HashiCorp Vault
#
# 4. Rotate passwords regularly (every 90 days recommended)
#
# 5. Enable 2FA for admin accounts in production
#
# ============================================================================
